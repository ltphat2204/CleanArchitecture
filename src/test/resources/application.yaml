spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: test
    password: #de trong

  jpa:
    hibernate:
      ddl-auto: create-drop #tao lai db moi khi chay
      database-platform: org.hibernate.dialect.H2Dialect
      show-sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG

